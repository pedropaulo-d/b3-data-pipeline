# ============================================================
# Dependências de DESENVOLVIMENTO / EXPLORAÇÃO (não-runtime)
# ============================================================
# NADA aqui faz parte do runtime do pipeline (ingestão, warehouse, dbt,
# Airflow). São ferramentas locais: notebooks exploratórios (Jupyter,
# plotly) e auditoria de segurança (pip-audit). NÃO vão para a imagem do
# Airflow e NÃO são auditadas pelo CI — dev local não é superfície de
# produção. Por isso CVEs em transitivas do Jupyter (bleach, tornado,
# jupyter-server) não falham o build.
#
# Para desenvolver / rodar notebooks, instale runtime + dev:
#   pip install -r requirements.txt -r requirements-dev.txt

# ------------------------------------------------------------
# Exploração — notebooks e visualização
# ------------------------------------------------------------
ipykernel>=6.29.0
jupyter>=1.0.0
plotly>=5.20.0

# ------------------------------------------------------------
# Segurança — auditoria de CVEs nas dependências Python. Roda no CI
# (.github/workflows/security.yml) sobre o runtime e localmente:
#   pip-audit -r requirements.txt
# ------------------------------------------------------------
pip-audit>=2.7.0

# ------------------------------------------------------------
# NÃO instaláveis via pip (são binários) — varredura de secrets:
#
#   gitleaks    -> https://github.com/gitleaks/gitleaks
#                  Windows:  scoop install gitleaks  (ou baixar release)
#                  Docker:   docker run --rm -v "${PWD}:/repo" \
#                              zricethezav/gitleaks:latest detect --source=/repo
#
#   trufflehog  -> https://github.com/trufflesecurity/trufflehog
#                  Docker:   docker run --rm -v "${PWD}:/repo" \
#                              trufflesecurity/trufflehog:latest git file:///repo
#
# O CI usa a action oficial do gitleaks; localmente o Docker basta.
# ------------------------------------------------------------
